export { fieldSurfaceBorderClassName as profileApplicationEntrySurfaceClassName } from '../../field-surface';

/** Tight stack for a profile import row and its optional “Previously submitted” diff. */
export const profileApplicationEntryGroupClassName = 'space-y-2';

/** Stack between entry groups in update/apply mode (room for diff panels and gap checkboxes). */
export const profileApplicationSectionStackClassName = 'space-y-4';

/** Tighter stack between entries in read-only submitted profile views. */
export const profileApplicationSectionStackReadClassName = 'space-y-2';
